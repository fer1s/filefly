// Body class applied while dragging: kills the .App grid-column transition (so the sidebar tracks
// the cursor instead of easing behind it) and forces the col-resize cursor everywhere. See index.css.
export const SIDEBAR_RESIZING_CLASS = "sidebar-resizing";
