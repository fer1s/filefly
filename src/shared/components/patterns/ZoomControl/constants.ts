// Percent-to-multiplier factor for the slider (its values are in whole percents, the zoom is a
// multiplier where 1 = 100%).
export const PERCENT_FACTOR = 100;

// Default slider granularity in percent when the consumer doesn't pass one.
export const DEFAULT_SLIDER_STEP = 1;
